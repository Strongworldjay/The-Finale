import {NavLink,Outlet,useLocation} from 'react-router-dom'; import {useEffect,useState} from 'react'; import {navGroups} from '../data/lore.js';
export default function Layout(){const [open,setOpen]=useState(false);const loc=useLocation();useEffect(()=>{setOpen(false);setTimeout(()=>{if(loc.hash)document.querySelector(loc.hash)?.scrollIntoView({behavior:'smooth',block:'start'});else window.scrollTo({top:0,behavior:'smooth'})},50)},[loc]);return <div className="app-shell">
<header className="topbar"><button className="icon-btn mobile" onClick={()=>setOpen(true)}>☰</button><NavLink to="/" className="brand"><span className="brand-mark">☼</span><span><b>HEÖFON</b><small>Archive of Radiance</small></span></NavLink><div className="top-actions"><button className="search-pill" onClick={()=>document.getElementById('site-search')?.focus()}>⌕ Search the archive</button></div></header>
<aside className={`sidebar ${open?'open':''}`}><button className="icon-btn close mobile" onClick={()=>setOpen(false)}>×</button><div className="side-intro">THE KINGDOM IN THE SKY</div>{navGroups.map(g=><nav key={g.label}><span className="nav-label">{g.label}</span>{g.items.map(([n,p])=><NavLink key={p} to={p} end={p==='/'}>{n}</NavLink>)}</nav>)}</aside>
<main><Outlet/><footer><span>HEÖFON · YEAR 269</span><span>Knowledge illuminates. Wisdom guides.</span></footer></main></div>}
